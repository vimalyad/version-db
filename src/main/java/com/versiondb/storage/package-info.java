/**
 * Storage layer: slotted pages, the disk manager, the buffer pool, heap files,
 * and the catalog. Everything above this layer reads and writes data
 * exclusively through the buffer pool.
 */
package com.versiondb.storage;
